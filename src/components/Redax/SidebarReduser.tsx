import React from 'react';
import {ActionTypes, FriendsType} from "./State";

const SidebarReduser = (action: ActionTypes, state: FriendsType[]) => {
    return state
};

export default SidebarReduser;